import { Layout } from "@components/layout";

const Chat = () => {
  return (
    <Layout
      hasGnbMenu
      headerProps={{
        title: "CHAT",
      }}
    >
      <div>Chat</div>
    </Layout>
  );
};

export default Chat;
