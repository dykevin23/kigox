import { Layout } from "@components/layout";

const Chat = () => {
  return (
    <Layout hasGnbMenu headerProps={{ left: "childSelector" }}>
      <div>Chat</div>
    </Layout>
  );
};

export default Chat;
