import { Layout } from "@components/layout";

const Social = () => {
  return (
    <Layout
      hasGnbMenu
      headerProps={{
        title: "SOCIAL",
      }}
    >
      <div>social</div>
    </Layout>
  );
};

export default Social;
