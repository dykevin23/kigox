import { Layout } from "@components/layout";

const Search = () => {
  return (
    <Layout hasGnbMenu headerProps={{ left: "goBack" }}>
      <div>search</div>
    </Layout>
  );
};

export default Search;
